<h1>Bem vindo a Kero Tudo Presentes!</h1>

<% if user_signed_in? %>
  <h2>Dashboard</h2>
  <% @celebrations.each do |c| %>
    <%= "#{c.day}/#{c.month} #{c.description}" %> <br>
  <% end %>
<% end %>
